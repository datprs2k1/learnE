import { FC } from 'react';
import { FlashCardForm as Form } from 'containers/index';
interface IFlashCardFormProps {}

export const FlashCardForm: FC<IFlashCardFormProps> = (props) => {
  return (
    <>
      <Form />
    </>
  );
};
